<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".InicioFragment">

    <Button
        android:id="@+id/btnFoto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Cargar Foto"
        android:textColorHint="@color/md_theme_light_background"
        />

    <Button
        android:id="@+id/btnVideo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Cargar video"
        android:textColorHint="@color/md_theme_light_background"
        android:layout_marginTop="100dp"
/>

    <Button
        android:id="@+id/btnReproRemota"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Reproducción remota"
        android:textColorHint="@color/md_theme_light_background"
        android:layout_marginTop="200dp"
        />

    <Button
        android:id="@+id/btnFotoRemota"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Foto remota"
        android:textColorHint="@color/md_theme_light_background"
        android:layout_marginTop="300dp"
        />
</FrameLayout>